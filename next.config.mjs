/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'via.placeholder.com',
            'picsum.photos',
            'developer.android.com',
            'upload.wikimedia.org',
        ],
    },
};

export default nextConfig;
